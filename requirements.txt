fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0
jinja2==3.1.4
pydantic==2.7.1
python-dotenv==1.0.1
# llama-cpp-python is NOT listed here — install it separately via the
# pre-built wheel command in README / starting.py to avoid needing MSVC.
